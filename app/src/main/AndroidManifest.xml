<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- V2.3: 彻底移除FOREGROUND_SERVICE_MEDIA_PROJECTION权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Android 14+ 前台服务 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- 语音识别权限 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="牌局智囊"
        android:supportsRtl="true"
        android:theme="@style/Theme.MjScreenshot"
        android:usesCleartextTraffic="true">

        <property
            android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
            android:value="Floating overlay window for poker game assistance" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="牌局智囊"
            android:theme="@style/Theme.MjScreenshot">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.action.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- V2.3: ScreenCaptureService不再作为前台服务，保留类定义供companion object使用 -->
        <service
            android:name=".ScreenCaptureService"
            android:exported="false" />

        <!-- HTTP服务器服务 -->
        <service
            android:name=".HttpServerService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

        <!-- 悬浮窗服务 -->
        <service
            android:name=".FloatingService"
            android:foregroundServiceType="specialUse"
            android:exported="false" />

        <!-- ★ 唯一截图方案：无障碍截图（不触发游戏黑屏检测）★ -->
        <service
            android:name=".PokerAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
